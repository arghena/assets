{
  "extends": ["@commitlint/config-conventional"],
  "defaultIgnores": false,
  "rules": {
    "scope-case": [2, "always", "lower-case"],
    "type-enum": [
      2,
      "always",
      ["chore", "ci", "docs", "refactor", "revert", "style"]
    ]
  }
}
